# RaceFrame PostgreSQL service

This folder is intended to live on the OCI VPS at:

`~/server-services/raceframe-postgres`

It runs the MVP PostgreSQL database for RaceFrame.

Current local routing:

- host bind: `127.0.0.1:5432`
- database: `raceframe`

Keep the real `.env` file on the server only.
