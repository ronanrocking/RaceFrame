# OpenSSH uses the first value it sees, so this file intentionally sorts
# before vendor/cloud drop-ins on distributions that set weaker defaults.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin no
X11Forwarding no
